<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 1. The Main Theme for your entire app -->
    <style name="Theme.CRS2025" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_blue</item>
    </style>

    <!-- 2. The Classic Splash Screen Theme -->
    <style name="Theme.App.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- This sets the solid background color that appears instantly -->
        <item name="android:windowBackground">@color/splash_background_blue</item>
        <!-- This ensures the status bar matches the background -->
        <item name="android:statusBarColor">@color/splash_background_blue</item>
    </style>

</resources>